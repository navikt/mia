<?xml version="1.0" encoding="UTF-8"?>
<application xmlns="http://appconfig.aura.nav.no"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://appconfig.aura.nav.no
             http://maven.adeo.no/xsd/appconfig.xsd ">

    <name>mia</name>
    <artifacts>
        <ear groupId="no.nav.fo" artifactId="mia-ear"/>
    </artifacts>

    <resources>
        <baseUrl mapToProperty="stilling.solr" alias="stilling.solr"/>
        <baseUrl mapToProperty="stillingsok.link" alias="stillingsok.link"/>
        <rest mapToProperty="miasolr.solr.ledigestillingercore" alias="miasolr.solr.ledigestillingercore"/>
        <rest mapToProperty="miasolr.solr.arbeidsledigecore" alias="miasolr.solr.arbeidsledigecore"/>
        <rest mapToProperty="miasolr.solr" alias="miasolr.solr"/>
        <fileLibrary alias="mia.ledetekster" mapToProperty="folder.ledetekster" />

        <directory name="mia.datadir" mapToProperty="mia.datadir">
            <!--<mountOnCifs alias="mia.cifs"/>-->
        </directory>
    </resources>

    <selftest>
        <path>/mia/internal/selftest.json</path>
        <humanReadablePath>/mia/internal/selftest</humanReadablePath>
    </selftest>

    <loadBalancer>
        <lbHealthMonitor type="HTTPS" isAliveUrl="mia/internal/isAlive" interval="5" timeout="16"/>
    </loadBalancer>

</application>